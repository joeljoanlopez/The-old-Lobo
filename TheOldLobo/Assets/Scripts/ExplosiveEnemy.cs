using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class ExplosiveEnemy 
{
    [SerializeField]
    GameObject Prefab;
    // Start is called before the first frame update
    void Start()
    {
        MakeOne();
    }

    // Update is called once per frame
    public void MakeOne()
    {
        
    }
}
